{
  "name": "kanban-cli-www",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Marketing/explainer site for kanban-cli.",
  "scripts": {
    "dev": "vite",
    "render:og": "bun scripts/render-og.ts",
    "build": "bun run render:og && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "typescript": "^5.9.3",
    "vite": "^5.4.0"
  }
}
