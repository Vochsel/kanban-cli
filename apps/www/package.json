{
  "name": "kanban-cli-www",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Marketing/explainer site for kanban-cli.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^5.4.0"
  }
}
